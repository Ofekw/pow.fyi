# Resources

Static media assets used by the app should live in this directory.

- Put images in `src/resources/images/`
- Put other media (audio/video/etc.) in `src/resources/media/`

Import assets via the `@/resources/...` path alias (for example, `import logo from '@/resources/images/logo.png';`) in React components when they are part of the app bundle.
